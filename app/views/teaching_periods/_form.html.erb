<%= form_for(@teaching_period) do |f| %>
  <% if @teaching_period.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teaching_period.errors.count, "error") %> prohibited this teaching_period from being saved:</h2>

      <ul>
      <% @teaching_period.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
